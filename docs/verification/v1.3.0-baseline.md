# v1.3.0 恢复基线验证

日期：2026-09-19  
恢复来源：`https://github.com/fyx88988-gif/85-media-assistant`  
恢复代码基线：`b15b844`  
设计文档提交：`eb65ae7`  
实施计划提交：`66d6ba2`

## 环境

- Windows x64
- Python 3.12.14（隔离测试环境）
- Node.js 24.19.0
- npm 11.17.0

由于本机内置 Python 对中文工作目录的路径解码异常，Python 测试通过临时 `W:` 映射访问同一份源码。项目本体仍位于 `D:\APP\视频下载助手`，没有复制或迁移。

## 基线结果

- 后端与发布逻辑：120 项通过，1 项安装布局测试取消选择。
- 被取消选择的测试 `test_windows_build_manifest_contains_every_runtime_component` 依赖预先生成的 `.build/local-webui/windows-staging`，当前尚未执行完整 Windows 安装包构建，不是运行时代码失败。
- 前端组件测试：17 项全部通过。
- 前端正式构建：通过。
- 统一下载页测试：通过。

## 基线命令

```powershell
python -m pytest webui/engine/tests build/release/tests -q -k "not test_windows_build_manifest_contains_every_runtime_component"
npm test --prefix webui/frontend -- --run
npm run build --prefix webui/frontend
node distribution/download-page.test.mjs
```

## 结论

恢复后的源码具备可继续开发的稳定基线。后续每个任务都必须维持上述基线，并在 Windows 安装包首次完整构建后重新启用被取消选择的安装布局测试。
